apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-project-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-project
  template:
    metadata:
      labels:
        app: kube-project
    spec:
      volumes:
        - name: files
          persistentVolumeClaim:
            claimName: kube-project-claim
      containers:
        - name: kube-project
          image: keskimaki/kube-project:0.6
          volumeMounts:
            - mountPath: /usr/src/app/files
              name: files
